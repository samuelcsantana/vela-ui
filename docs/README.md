# docs/

Supplementary documentation for this repository, read on demand by Claude Code (not preloaded like `CLAUDE.md`). Current contents:

- `architecture-planning.md` — source-of-truth architecture rules for the front-end (state separation, folder structure, forms, performance conventions).
- `ai-development-logs.md` — short pointer notes for AI agents (commands, Rsbuild/Rspack doc links).

The high-level summary of both is already folded into the root `CLAUDE.md`; consult these files directly for full detail.
